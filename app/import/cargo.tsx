import { router } from "expo-router";
import { BookingScreen, BookingSection, ChoiceTile, FormField, OptionalDetails } from "@/components/booking/booking-ui";
import { importSteps } from "@/lib/service-booking";
import { useBookingDraft } from "@/stores/booking-draft";

export default function ImportCargoScreen() { const { importDraft, updateImportDraft } = useBookingDraft(); const categories = [{ id: "general", title: "General cargo", icon: "package-variant-closed" }, { id: "commercial", title: "Commercial stock", icon: "storefront-outline" }, { id: "personal", title: "Personal items", icon: "account-box-outline" }]; return <BookingScreen activeStep="cargo" serviceLabel="International Imports" progressSteps={importSteps} title="What are you importing?" detail="Choose the closest cargo type. We will confirm final requirements later." continueLabel="Continue to consignee" continueDisabled={!importDraft.cargoCategory} onContinue={() => router.push("/import/consignee" as never)}><BookingSection>{categories.map((category) => <ChoiceTile key={category.id} title={category.title} detail="Select cargo type" icon={category.icon as any} selected={importDraft.cargoCategory === category.id} onPress={() => updateImportDraft({ cargoCategory: category.id })} />)}</BookingSection><OptionalDetails label="Add cargo description"><FormField label="Description" icon="text-box-outline" placeholder="Optional item details" value={importDraft.cargoDescription ?? ""} onChangeText={(cargoDescription) => updateImportDraft({ cargoDescription })} /></OptionalDetails></BookingScreen>; }
