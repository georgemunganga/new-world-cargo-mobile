import { useEffect, useState } from "react";
import { KeyboardAvoidingView, LayoutAnimation, Platform, ScrollView, StyleSheet, Text, TouchableOpacity, UIManager, View, useWindowDimensions } from "react-native";
import { router, type Href } from "expo-router";
import { useSafeAreaInsets } from "react-native-safe-area-context";
import { BookingSection, FormField } from "@/components/booking/booking-ui";
import { RouteEntryCard } from "@/components/booking/route-entry-card";
import { LocalDeliveryMapBackdrop, type PickupPinPosition } from "@/components/map/local-delivery-map-backdrop";
import { AppIcon } from "@/components/ui/app-icon";
import { IconButton, PrimaryButton, Screen } from "@/components/ui/nwc-ui";
import { isRouteReady } from "@/lib/booking-progress";
import { getLocalDeliveryQuote } from "@/lib/local-delivery-quote";
import { getLocalDeliveryRouteSheetState, type LocalDeliveryRouteTarget } from "@/lib/local-delivery-route-sheet";
import { savedPlaces } from "@/lib/mock-cargo-data";
import { nwcColors } from "@/lib/nwc-theme";
import { routeSuggestionToAddress } from "@/lib/route-autocomplete";
import { useBookingDraft } from "@/stores/booking-draft";
import type { Address, LocalDeliveryVehicle } from "@/types/cargo";

export default function LocalDeliveryRouteScreen() {
  const { height } = useWindowDimensions();
  const insets = useSafeAreaInsets();
  const { localDraft, updateLocalDraft, setBookingStep } = useBookingDraft();
  const [activeTarget, setActiveTarget] = useState<LocalDeliveryRouteTarget | null>(null);
  const [showManualEntry, setShowManualEntry] = useState(false);
  const [adjustingTarget, setAdjustingTarget] = useState<"pickup" | "destination" | null>(null);
  const [pickupPinPosition, setPickupPinPosition] = useState<PickupPinPosition>("initial");
  const [destinationPinPosition, setDestinationPinPosition] = useState<PickupPinPosition>("initial");
  const routeReady = isRouteReady(localDraft);
  const vehicle = localDraft.vehicle ?? "scooter";
  const quote = getLocalDeliveryQuote(localDraft.pickup, localDraft.destination, vehicle);
  const sheetState = getLocalDeliveryRouteSheetState(activeTarget, height - insets.top, Boolean(adjustingTarget));
  useEffect(() => { if (Platform.OS === "android" && UIManager.setLayoutAnimationEnabledExperimental) UIManager.setLayoutAnimationEnabledExperimental(true); LayoutAnimation.configureNext(LayoutAnimation.Presets.easeInEaseOut); }, [sheetState.height]);
  const updateAddress = (key: "pickup" | "destination", detail: string) => { const current = localDraft[key] ?? { city: "Lusaka", area: "" }; updateLocalDraft({ [key]: { ...current, detail } }); };
  const applySaved = (place: Address, key: "pickup" | "destination") => updateLocalDraft({ [key]: place });
  const continueBooking = () => { setBookingStep("parcel"); router.push("/local-delivery/parcel" as Href); };
  const openPinAdjustment = (target: "pickup" | "destination") => { setActiveTarget(null); setShowManualEntry(false); setAdjustingTarget(target); };
  const selectPinPosition = (position: PickupPinPosition) => { if (adjustingTarget === "pickup") setPickupPinPosition(position); else setDestinationPinPosition(position); };
  const isEditing = Boolean(activeTarget);
  const sheetTitle = isEditing ? "Where should we look?" : adjustingTarget ? `Adjust ${adjustingTarget} pin` : "Set your route";
  const sheetDetail = isEditing ? "Suggestions appear directly beneath the field you are typing in." : adjustingTarget ? "Use the controls to nudge the mock map pin, then confirm its position." : "Choose pickup and destination. We will handle the rest.";

  return <Screen><KeyboardAvoidingView behavior={Platform.OS === "ios" ? "padding" : undefined} style={styles.page}><LocalDeliveryMapBackdrop pickup={localDraft.pickup} destination={localDraft.destination} pickupPinPosition={pickupPinPosition} destinationPinPosition={destinationPinPosition} adjustingTarget={adjustingTarget} routeReady={routeReady} /><View style={styles.topBar}><IconButton label="Go back" icon="arrow-left" onPress={() => router.back()} /><View style={styles.topPill}><View style={styles.topPillDot} /><Text style={styles.topPillText}>Local Delivery</Text></View><IconButton label="Open notifications" icon="bell-outline" onPress={() => router.push("/notifications" as Href)} /></View><View style={[styles.sheet, { height: sheetState.height }]}><View style={styles.grabber} /><ScrollView keyboardDismissMode="on-drag" keyboardShouldPersistTaps="handled" showsVerticalScrollIndicator={false} contentContainerStyle={[styles.sheetContent, { paddingBottom: Math.max(insets.bottom, 18) + 10 }]}><View><Text style={styles.overline}>{sheetState.mode === "editing" ? `Search ${activeTarget === "from" ? "pickup" : "destination"}` : sheetState.mode === "adjusting-pin" ? `${adjustingTarget === "pickup" ? "Pickup" : "Destination"} location` : "Local Delivery"}</Text><Text style={styles.title}>{sheetTitle}</Text><Text style={styles.detail}>{sheetDetail}</Text></View>{adjustingTarget ? <PinAdjustmentPanel target={adjustingTarget} position={adjustingTarget === "pickup" ? pickupPinPosition : destinationPinPosition} onNudge={selectPinPosition} onConfirm={() => setAdjustingTarget(null)} /> : <><RouteEntryCard scope="local" from={{ value: localDraft.pickup?.detail ?? "", detail: localDraft.pickup?.area ?? "" }} to={{ value: localDraft.destination?.detail ?? "", detail: localDraft.destination?.area ?? "" }} onSuggestionSelect={(target, suggestion) => updateLocalDraft({ [target === "from" ? "pickup" : "destination"]: routeSuggestionToAddress(suggestion) })} onActiveTargetChange={(target) => { setActiveTarget(target); if (target) setAdjustingTarget(null); }} onManualEntryPress={() => { setShowManualEntry((current) => !current); }} accessibilityHint="Search nearby local pickup and destination locations" />{isEditing ? null : <><RoutePreview quote={quote} vehicle={vehicle} onSelectVehicle={(nextVehicle) => updateLocalDraft({ vehicle: nextVehicle })} onAdjustPickup={() => openPinAdjustment("pickup")} onAdjustDestination={() => openPinAdjustment("destination")} /><TouchableOpacity accessibilityRole="button" accessibilityState={{ expanded: showManualEntry }} accessibilityLabel={showManualEntry ? "Hide manual address entry" : "Enter an address manually"} onPress={() => setShowManualEntry((current) => !current)} style={styles.manualToggle}><AppIcon name="pencil-outline" size={18} color={nwcColors.info} /><Text style={styles.manualToggleText}>{showManualEntry ? "Hide manual address entry" : "Enter address manually"}</Text><AppIcon name={showManualEntry ? "chevron-up" : "chevron-down"} size={19} color={nwcColors.info} /></TouchableOpacity>{showManualEntry ? <View style={styles.manualArea}><BookingSection label="Pickup details"><FormField label="From where?" icon="circle-outline" placeholder="Building, street, area, or landmark" value={localDraft.pickup?.detail ?? ""} onChangeText={(detail) => updateAddress("pickup", detail)} /><FormField label="Pickup area" icon="map-marker-outline" placeholder="e.g. Longacres, Lusaka" value={localDraft.pickup?.area ?? ""} onChangeText={(area) => updateLocalDraft({ pickup: { city: "Lusaka", detail: localDraft.pickup?.detail ?? "", area } })} /></BookingSection><BookingSection label="Delivery details"><FormField label="To where?" icon="map-marker" placeholder="Building, street, area, or landmark" value={localDraft.destination?.detail ?? ""} onChangeText={(detail) => updateAddress("destination", detail)} /><FormField label="Delivery area" icon="map-marker-outline" placeholder="e.g. Roma, Lusaka" value={localDraft.destination?.area ?? ""} onChangeText={(area) => updateLocalDraft({ destination: { city: "Lusaka", detail: localDraft.destination?.detail ?? "", area } })} /></BookingSection></View> : null}<View style={styles.savedLine}><Text style={styles.savedTitle}>Saved places</Text><View style={styles.savedChips}>{savedPlaces.slice(0, 3).map((place) => <TouchableOpacity key={place.label} accessibilityRole="button" accessibilityLabel={`Use ${place.label} as ${localDraft.pickup ? "destination" : "pickup"}`} onPress={() => applySaved(place, localDraft.pickup ? "destination" : "pickup")} style={styles.savedChip}><Text style={styles.savedChipText}>{place.label}</Text></TouchableOpacity>)}</View></View><PrimaryButton label={routeReady ? "Continue to parcel" : "Add pickup and destination"} icon="arrow-right" disabled={!routeReady} onPress={continueBooking} /></>}</>}</ScrollView></View></KeyboardAvoidingView></Screen>;
}

function RoutePreview({ quote, vehicle, onSelectVehicle, onAdjustPickup, onAdjustDestination }: { quote: ReturnType<typeof getLocalDeliveryQuote>; vehicle: LocalDeliveryVehicle; onSelectVehicle: (vehicle: LocalDeliveryVehicle) => void; onAdjustPickup: () => void; onAdjustDestination: () => void }) {
  if (!quote) return null;
  return <View style={styles.routePreview}><View style={styles.routePreviewTop}><View style={styles.quoteIcon}><AppIcon name="bike-fast" size={20} color={nwcColors.primaryInk} /></View><View style={styles.quoteCopy}><Text style={styles.quoteLabel}>Local delivery estimate</Text><Text style={styles.quoteMeta}>{quote.distance} · {quote.eta}</Text></View><Text style={styles.quotePrice}>{quote.price}</Text></View><View style={styles.vehicleChoices}>{(["scooter", "small_van", "cargo_van"] as LocalDeliveryVehicle[]).map((choice) => <TouchableOpacity key={choice} accessibilityRole="button" accessibilityState={{ selected: vehicle === choice }} accessibilityLabel={`Choose ${vehicleLabels[choice]}`} onPress={() => onSelectVehicle(choice)} style={[styles.vehicleChoice, vehicle === choice && styles.vehicleChoiceActive]}><AppIcon name={choice === "scooter" ? "bike-fast" : choice === "small_van" ? "van-utility" : "truck-outline"} size={17} color={vehicle === choice ? nwcColors.primaryInk : nwcColors.info} /><Text style={[styles.vehicleName, vehicle === choice && styles.vehicleNameActive]}>{vehicleLabels[choice]}</Text></TouchableOpacity>)}</View><Text style={styles.capacityText}>{quote.vehicleLabel} · {quote.capacity}</Text><View style={styles.routePreviewFooter}><Text style={styles.quoteArrival}>{quote.arrivalWindow}</Text><View style={styles.pinLinks}><TouchableOpacity accessibilityRole="button" accessibilityLabel="Adjust pickup pin" onPress={onAdjustPickup} style={styles.adjustLink}><AppIcon name="crosshairs-gps" size={16} color={nwcColors.info} /><Text style={styles.adjustLinkText}>Pickup</Text></TouchableOpacity><TouchableOpacity accessibilityRole="button" accessibilityLabel="Adjust destination pin" onPress={onAdjustDestination} style={styles.adjustLink}><AppIcon name="map-marker-radius-outline" size={16} color={nwcColors.info} /><Text style={styles.adjustLinkText}>Destination</Text></TouchableOpacity></View></View></View>;
}

function PinAdjustmentPanel({ target, position, onNudge, onConfirm }: { target: "pickup" | "destination"; position: PickupPinPosition; onNudge: (position: PickupPinPosition) => void; onConfirm: () => void }) {
  const targetLabel = target === "pickup" ? "pickup" : "destination";
  return <View style={styles.pinPanel}><View style={styles.pinHint}><AppIcon name="map-marker-radius-outline" size={19} color={nwcColors.info} /><Text style={styles.pinHintText}>Mock map control. Choose a nearby {targetLabel} point.</Text></View><View style={styles.nudgeGrid}><TouchableOpacity accessibilityRole="button" accessibilityLabel={`Move ${targetLabel} pin north`} onPress={() => onNudge("north")} style={[styles.nudgeButton, position === "north" && styles.nudgeButtonActive]}><AppIcon name="arrow-up" size={21} color={nwcColors.brandNavy} /></TouchableOpacity><View style={styles.nudgeMiddle}><TouchableOpacity accessibilityRole="button" accessibilityLabel={`Move ${targetLabel} pin west`} onPress={() => onNudge("west")} style={[styles.nudgeButton, position === "west" && styles.nudgeButtonActive]}><AppIcon name="arrow-left" size={21} color={nwcColors.brandNavy} /></TouchableOpacity><TouchableOpacity accessibilityRole="button" accessibilityLabel={`Reset ${targetLabel} pin`} onPress={() => onNudge("initial")} style={[styles.nudgeButton, position === "initial" && styles.nudgeButtonActive]}><AppIcon name="crosshairs-gps" size={20} color={nwcColors.brandNavy} /></TouchableOpacity><TouchableOpacity accessibilityRole="button" accessibilityLabel={`Move ${targetLabel} pin east`} onPress={() => onNudge("east")} style={[styles.nudgeButton, position === "east" && styles.nudgeButtonActive]}><AppIcon name="arrow-right" size={21} color={nwcColors.brandNavy} /></TouchableOpacity></View><TouchableOpacity accessibilityRole="button" accessibilityLabel={`Move ${targetLabel} pin south`} onPress={() => onNudge("south")} style={[styles.nudgeButton, position === "south" && styles.nudgeButtonActive]}><AppIcon name="arrow-down" size={21} color={nwcColors.brandNavy} /></TouchableOpacity></View><PrimaryButton label={`Confirm ${targetLabel} pin`} icon="check" onPress={onConfirm} /></View>;
}

const styles = StyleSheet.create({
  page: { flex: 1, backgroundColor: "#EAF0F1" },
  topBar: { position: "absolute", zIndex: 3, top: 14, left: 18, right: 18, flexDirection: "row", alignItems: "center", justifyContent: "space-between" }, topPill: { minHeight: 40, paddingHorizontal: 13, borderRadius: 14, flexDirection: "row", alignItems: "center", gap: 7, backgroundColor: "rgba(255,255,255,0.95)" }, topPillDot: { height: 8, width: 8, borderRadius: 4, backgroundColor: nwcColors.primary }, topPillText: { color: nwcColors.brandNavy, fontSize: 12, lineHeight: 17, fontFamily: "Poppins_800ExtraBold" },
  sheet: { position: "absolute", zIndex: 5, left: 0, right: 0, bottom: 0, borderTopLeftRadius: 30, borderTopRightRadius: 30, overflow: "hidden", backgroundColor: nwcColors.background, shadowColor: "#012642", shadowOffset: { width: 0, height: -7 }, shadowOpacity: 0.15, shadowRadius: 18, elevation: 13 }, grabber: { alignSelf: "center", width: 43, height: 5, borderRadius: 3, backgroundColor: "#C5D1D6", marginTop: 10, marginBottom: 7 }, sheetContent: { flexGrow: 1, paddingHorizontal: 20, gap: 14 },
  overline: { color: nwcColors.info, fontSize: 11, lineHeight: 15, letterSpacing: 0.8, textTransform: "uppercase", fontFamily: "Poppins_800ExtraBold" }, title: { color: nwcColors.foreground, fontSize: 25, lineHeight: 31, fontFamily: "Poppins_800ExtraBold", marginTop: 1 }, detail: { color: nwcColors.muted, fontSize: 13, lineHeight: 19, fontFamily: "Poppins_500Medium", marginTop: 2 },
  routePreview: { gap: 9, padding: 13, borderRadius: 18, borderWidth: 1, borderColor: "#DCE8EC", backgroundColor: "#F4F9FA" }, routePreviewTop: { flexDirection: "row", alignItems: "center", gap: 9 }, quoteIcon: { width: 38, height: 38, borderRadius: 13, alignItems: "center", justifyContent: "center", backgroundColor: nwcColors.primary }, quoteCopy: { flex: 1 }, quoteLabel: { color: nwcColors.foreground, fontSize: 12, lineHeight: 16, fontFamily: "Poppins_800ExtraBold" }, quoteMeta: { color: nwcColors.muted, fontSize: 11, lineHeight: 16, fontFamily: "Poppins_600SemiBold" }, quotePrice: { color: nwcColors.brandNavy, fontSize: 19, lineHeight: 25, fontFamily: "Poppins_800ExtraBold" }, vehicleChoices: { flexDirection: "row", gap: 6 }, vehicleChoice: { flex: 1, minHeight: 55, borderRadius: 13, borderWidth: 1, borderColor: "#DDE7EA", backgroundColor: nwcColors.white, alignItems: "center", justifyContent: "center", gap: 2 }, vehicleChoiceActive: { borderColor: nwcColors.primary, backgroundColor: "#FFF3CC" }, vehicleName: { color: nwcColors.info, fontSize: 10, lineHeight: 14, fontFamily: "Poppins_800ExtraBold" }, vehicleNameActive: { color: nwcColors.primaryInk }, capacityText: { color: nwcColors.muted, fontSize: 11, lineHeight: 15, fontFamily: "Poppins_600SemiBold" }, routePreviewFooter: { paddingTop: 9, borderTopWidth: 1, borderTopColor: "#E1EAED", flexDirection: "row", alignItems: "center", justifyContent: "space-between", gap: 8 }, quoteArrival: { flex: 1, color: nwcColors.info, fontSize: 11, lineHeight: 16, fontFamily: "Poppins_700Bold" }, pinLinks: { flexDirection: "row", gap: 5 }, adjustLink: { minHeight: 32, borderRadius: 10, paddingHorizontal: 8, alignItems: "center", justifyContent: "center", flexDirection: "row", gap: 4, backgroundColor: nwcColors.white }, adjustLinkText: { color: nwcColors.info, fontSize: 11, lineHeight: 15, fontFamily: "Poppins_800ExtraBold" },
  manualToggle: { minHeight: 42, borderRadius: 14, paddingHorizontal: 12, backgroundColor: "#EAF4F8", flexDirection: "row", alignItems: "center", gap: 8 }, manualToggleText: { flex: 1, color: nwcColors.info, fontSize: 12, lineHeight: 17, fontFamily: "Poppins_800ExtraBold" }, manualArea: { gap: 11 }, savedLine: { gap: 8 }, savedTitle: { color: nwcColors.foreground, fontSize: 12, lineHeight: 17, fontFamily: "Poppins_800ExtraBold" }, savedChips: { flexDirection: "row", gap: 7 }, savedChip: { minHeight: 34, borderRadius: 12, paddingHorizontal: 11, alignItems: "center", justifyContent: "center", backgroundColor: nwcColors.surface, borderWidth: 1, borderColor: nwcColors.border }, savedChipText: { color: nwcColors.brandNavy, fontSize: 12, lineHeight: 16, fontFamily: "Poppins_700Bold" },
  pinPanel: { gap: 12 }, pinHint: { minHeight: 43, paddingHorizontal: 11, borderRadius: 14, flexDirection: "row", alignItems: "center", gap: 8, backgroundColor: "#EAF4F8" }, pinHintText: { flex: 1, color: nwcColors.info, fontSize: 12, lineHeight: 17, fontFamily: "Poppins_700Bold" }, nudgeGrid: { alignItems: "center", gap: 7 }, nudgeMiddle: { flexDirection: "row", gap: 7 }, nudgeButton: { width: 43, height: 37, alignItems: "center", justifyContent: "center", borderRadius: 12, backgroundColor: nwcColors.white, borderWidth: 1, borderColor: nwcColors.border }, nudgeButtonActive: { borderColor: nwcColors.primary, backgroundColor: "#FFF1C5" },
});

const vehicleLabels: Record<LocalDeliveryVehicle, string> = { scooter: "Bike", small_van: "Small van", cargo_van: "Cargo van" };
